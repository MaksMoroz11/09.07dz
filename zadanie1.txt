1. name = "Tom"
	1) Вычисляется выражение "Tom", получается объект типа str со значением "Tom".
	2) Имя name начинает ссылаться на этот объект.
	3) Имя name ссылается на объект типа str со значением "Tom".
	4) Уже существующий объект не изменяется, так как объект только что создан.

2. age = 20
	1) Вычисляется выражение 20, получается объект типа int со значением 20.
	2) Имя age начинает ссылаться на этот объект.
	3) Имя name ссылается на объект типа str со значением "Tom". Имя age ссылается на объект типа int со значением 20.
	4) Уже существующий объект не изменяется.

3. message = name + " is " + str(age)
	1) Вычисляется выражение name + " is " + str(age), получается новый объект типа str со значением "Tom is 20".
	2) Имя message начинает ссылаться на этот объект.
	3) Имя name ссылается на объект типа str со значением "Tom". Имя age ссылается на объект типа int со значением 20. Имя message ссылается на объект типа str со значением "Tom is 20".
	4) Уже существующий объект не изменяется. Объект по ссылке name остался неизменным, так как объекты типа str неизменяемые.

4. age = age + 1
	1) Вычисляется выражение age + 1, получается новый объект типа int со значением 21.
	2) Имя age теперь ссылается на объект 21, а не на объект 20.
	3) Имя name ссылается на объект типа str со значением "Tom". Имя age ссылается на объект типа int со значением 21. Имя message ссылается на объект типа str со значением "Tom is 20".
	4) Уже существующий объект не изменяется. Объект типа int со значением 20 остался неизменным, так как объекты типа int неизменяемые.

5. name = name + " Smith"
	1) Вычисляется выражение name + " Smith", получается новый объект типа str со значением "Tom Smith".
	2) Имя name теперь ссылается на этот новый объект.
	3) Имя name ссылается на объект типа str со значением "Tom Smith". Имя age ссылается на объект типа int со значением 21. Имя message ссылается на объект типа str со значением "Tom is 20".
	4) Уже существующий объект не изменяется. Объект со значением "Tom" остался неизменным, так как объекты типа str неизменяемые.

6. final_message = name + " is " + str(age)
	1) Вычисляется выражение name + " is " + str(age), получается новый объект типа str со значением "Tom Smith is 21".
	2) Имя final_message начинает ссылаться на этот объект.
	3) Имя name ссылается на объект типа str со значением "Tom Smith". Имя age ссылается на объект типа int со значением 21. Имя message ссылается на объект типа str со значением "Tom is 20". Имя final_message ссылается на объект типа str со значением "Tom Smith is 21".
	4) Уже существующий объект не изменяется.
